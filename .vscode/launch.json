{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug: new-api",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "env": {
        "GIN_MODE": "debug"
      }
    }
  ]
}
